name: Build QMK (Silakka54)
on:
  push:
    paths:
      - 'qmk/**'
      - '.github/workflows/build-qmk.yml'
    tags:
      - 'qmk-*'
  pull_request:
    paths:
      - 'qmk/**'
      - '.github/workflows/build-qmk.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: qmkfm/qmk_firmware:latest
    steps:
      - uses: actions/checkout@v4
        with:
          path: keymap

      - name: Copy keyboard and compile
        env:
          QMK_HOME: /tmp/qmk_fw
          QMK_FIRMWARE: /tmp/qmk_fw
        run: |
          echo "=== Setup fresh QMK firmware ==="
          qmk setup -y -H /tmp/qmk_fw 2>&1
          echo "=== Copy keymap ==="
          cp -r keymap/qmk/keyboards/silakka54/keymaps /tmp/qmk_fw/keyboards/silakka54/
          ls -la /tmp/qmk_fw/keyboards/silakka54/keymaps/default/
          echo "=== Compiling ==="
          cd /tmp/qmk_fw
          make silakka54:default
          echo "=== Build output ==="
          ls -la .build/ 2>/dev/null || echo "No .build/"
          find . -name "*.hex" -o -name "*.uf2" -o -name "*.bin" 2>/dev/null || echo "No firmware found"

      - name: Locate firmware
        id: locate
        run: |
          cd /tmp/qmk_fw
          mkdir -p /tmp/firmware
          echo "=== Searching for firmware files ==="
          find /tmp/qmk_fw -name "*.uf2" -o -name "*.bin" -o -name "*.hex" 2>/dev/null || echo "None found"
          echo "=== Copying firmware ==="
          find /tmp/qmk_fw -name "*.uf2" -o -name "*.bin" -o -name "*.hex" -exec cp {} /tmp/firmware/ \; 2>/dev/null || true
          ls -la /tmp/firmware/ || echo "Empty"

      - name: Upload firmware artifact
        uses: actions/upload-artifact@v4
        with:
          name: qmk-silakka54-firmware
          path: /tmp/firmware/

      - name: Publish release
        if: startsWith(github.ref, 'refs/tags/qmk-')
        uses: softprops/action-gh-release@v2
        with:
          files: /tmp/firmware/*
          generate_release_notes: true
